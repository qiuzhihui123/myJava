First(Install): 
    1) We can use the git which is internally installed in Cmder;
    2) We can download it online;
    3) If you use the Linux OS you can installed by instruction "apt-get install git";
    
Second(using):
    1) 和svn相比git的优势有：
        Ⅰ.git 是分布式存储版本的工具 
        Ⅱ.git支持离线更新到本地的版本控制仓库
    
    2）配置用户名，邮箱:
        Ⅰ.config --global user.name(name替换成用户名)   config --global user.email(email替换成邮箱) 
        
    
    初始化git路径：
        Ⅰ.
