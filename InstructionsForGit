First(Install): 
    1) We can use the git which is internally installed in Cmder;
    2) We can download it online;
    3) If you use the Linux OS you can installeb by instruction "apt-get install git";
    
   
